$('#like-icon-item-<%= @item.id.to_s %>').

  html('<i class="fa fa-heart"></i><%= link_to "いいね！", item_like_path(@item.id, @like), method: :DELETE, remote: true, class: "product-box__bottom__like hide-text" %><%= @item.likes.count %>');
